import * as React from 'react'

import LayoutBundleGalleryClient from './index.client'

const LayoutBundleGallery: React.FC = () => {
  return <LayoutBundleGalleryClient />
}

export default LayoutBundleGallery
