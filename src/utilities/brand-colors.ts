/** Storeframe / corporate brand red — solid for accents that must not fade. */
export const BRAND_RED = '#a51917'

/** Brand red at 80% opacity — titles, borders, glows, hover fills. */
export const BRAND_RED_80 = 'rgba(165, 25, 23, 0.8)'

/** Brand red at 40% opacity — meteor glows. */
export const BRAND_RED_40 = 'rgba(165, 25, 23, 0.4)'

/** Brand red at 35% opacity — meteor trails. */
export const BRAND_RED_35 = 'rgba(165, 25, 23, 0.35)'

/** Brand red at 14% opacity — section radial washes. */
export const BRAND_RED_14 = 'rgba(165, 25, 23, 0.14)'

/** Brand red at 12% opacity — section radial washes. */
export const BRAND_RED_12 = 'rgba(165, 25, 23, 0.12)'

/** RGB 0–1 tuple for WebGL / cobe globe markers. */
export const BRAND_RED_RGB: [number, number, number] = [
  165 / 255,
  25 / 255,
  23 / 255,
]
